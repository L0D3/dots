<!DOCTYPE html>
<html lang="<%= (I18n.locale || I18n.default_locale) %>">
   <head>
    <meta charset="utf-8">
    <title>Ips Patentverwaltung</title>

    <meta name="description" content="Ips Patentverwaltung">
    <meta name="author" content="Johannes Lahann">
    <%= stylesheet_link_tag "reset", "bootstrap.min", "bootstrap-responsive.min", "datepicker", "timepicker", "beautiful-scaffold", "tagit-dark-grey", "colorpicker", "bootstrap-wysihtml5" %>
    <style>
        body {
            padding-top: 60px; /* 60px to make the container go all the way to the bottom of the topbar */
        }
    </style>

    

    <%= csrf_meta_tags %>
    <%= yield :head %>
  </head>
  <body>
    <div class="container-fluid">
      <div class="navbar navbar-fixed-top">
        <div class="navbar-inner">
          <div class="container">
            <a class="brand" href="#">
             IPS 
            </a>
            <ul class="nav">
                  <%= render :partial => "layouts/beautiful_menu" %>
            </ul>
            <ul class="nav pull-right">
              <li>
                <!-- Signin - Do not remove -->
              </li>
            </ul>
          </div>
        </div>
      </div>

      <div class="row-fluid">
          <!-- Zone PJAX -->
          <% if not flash[:notice].blank? then %>
            <div class="alert alert-info">
              <a class="close" data-dismiss="alert" href="#">×</a>
              <h4 class="alert-heading">Info : </h4>
              <%= flash[:notice] %>
            </div>
          <% end %>
          <% if not flash[:error].blank? then %>
            <div class="alert alert-error">
              <a class="close" data-dismiss="alert" href="#">×</a>
              <h4 class="alert-heading">Info : </h4>
              <%= flash[:error] %>
            </div>
          <% end %>
          <div id="pjax-container">
            <%= yield %>
          </div>
          <!-- FIN Zone PJAX -->
        </div>
    </div>

    <div class="loader" style="display:none">
      <div class="circle"></div>
      <div class="circle1"></div>
    </div>

    <!--Modal - Do not remove -->

     </body>
</html>
