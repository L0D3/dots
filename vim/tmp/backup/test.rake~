task :test_controls => :environment do
  Patent.all.each do |p|
    puts "Azahl controls:"
    puts p.controls.size.to_s
    puts "anmeldedatum #{p.anmeldedatum.to_s}"
    puts "fälligkeit #{p.gebührenfälligkeit(Date.today.year).to_s}"
  end
  end
